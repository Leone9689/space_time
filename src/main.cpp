#include "keyslice_tracker.h"
 
int main(int argc, char* argv[])
{
  //  cv::Mat bluekeySlice;
  //std::vector<cv::Mat> rgbKeyframe;
  KeySlice keyslice;
  //bluekeySlice = keyslice.keySlice;
  //cv::namedWindow("Key slice");                                   
  //cv::imshow("Key slice", bluekeySlice);  
 // cv::waitKey(); 
} 
